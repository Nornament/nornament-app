-- A "graph" is the setting drawing the karigar works from - stone placement
-- laid out on graph paper. It belongs to the design, alongside the CAD.
ALTER TYPE app.media_kind ADD VALUE IF NOT EXISTS 'GRAPH';;
