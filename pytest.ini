[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = test_*.py
addopts = -q --strict-markers
markers =
    golden: compares this app's figures against CSV exported from the legacy SQL views
